{
  "name": "spring-tools-claude-plugin-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Development checks for the Spring Tools Claude Code plugin (explanation playbooks, plugin configuration). Not part of the published plugin.",
  "license": "EPL-1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node check-explanations.mjs --require-all --todo",
    "check:links": "node check-explanations.mjs --require-all --links",
    "test": "node --test --test-reporter=tap \"test/*.test.mjs\""
  }
}
